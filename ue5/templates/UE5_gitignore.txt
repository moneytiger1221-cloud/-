# ============================================================
# UE5 プロジェクト用 .gitignore
# → C:\UE\SpeedTest\.gitignore として保存してください
#   （ファイル名の先頭のドットを忘れずに。拡張子は付けない）
# ============================================================

# --- 自動生成される巨大フォルダ（絶対にコミットしない） ---
# これらを入れると数十GBになり、Gitが実用不能になります。
# 全部削除してもUE5が起動時に作り直すので、失うものはありません。
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/

# --- IDE / エディタ ---
.vs/
.vscode/
.idea/
*.sln
*.suo
*.opensdf
*.sdf
*.VC.db
*.VC.opendb

# --- OS が勝手に作るファイル ---
.DS_Store
Thumbs.db
desktop.ini

# --- プラグインのビルド成果物 ---
Plugins/**/Binaries/
Plugins/**/Intermediate/

# --- Claude Code のローカル設定（個人ごとの設定なので共有しない） ---
.claude/settings.local.json

# --- 大きすぎる中間出力 ---
*.pdb
*.obj

# ============================================================
# 注意：Content/ は除外しません。
# ここがあなたの資産（モデル、マテリアル、レベル）です。
# ただし .uasset は巨大バイナリなので、
# 必ず .gitattributes で Git LFS に振ってください。
# ============================================================
